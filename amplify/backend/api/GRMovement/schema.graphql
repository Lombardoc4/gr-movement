type Person @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstName: String!
  lastName: String!
  foreverAge: String
  state: String
}

type ImgPerson @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  image_id: String
  forever_age: Int
  description: String
}
 